# @package model
_target_: src.models.sync_model.create_model

# Model configuration
name: "cross_correlation"  # cross_correlation, neural
audio_feature_dim: 13
visual_feature_dim: 128
hidden_dim: 512
dropout: 0.1
num_layers: 2
